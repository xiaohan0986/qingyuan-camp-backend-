# 操作日志数据库设置

## 📋 功能说明

操作日志系统已升级为数据库存储，具备以下特性：

### ✅ 核心功能
1. **数据库存储** - 日志保存在 MySQL 数据库中
2. **自动清理** - 自动删除 30 天前的日志
3. **密码保护** - 每次访问都需要输入密码
4. **批量操作** - 支持批量复制和删除

### 🔐 访问密码
```
hb098634
```

---

## 🚀 安装步骤

### 1. 创建数据表

在浏览器中访问：
```
http://localhost/qianwutong/create_operation_logs_table.php
```

页面会显示：
```
✅ 操作日志表创建成功！
表名：operation_logs
```

### 2. 验证安装

访问操作日志页面：
```
http://localhost/qianwutong/admin/diagnosis/operation_log.php
```

输入密码 `hb098634` 登录。

---

## 📊 数据库表结构

```sql
CREATE TABLE `operation_logs` (
    `id` INT AUTO_INCREMENT PRIMARY KEY,
    `timestamp` DATETIME NOT NULL,
    `username` VARCHAR(50) NOT NULL,
    `ip` VARCHAR(50) NOT NULL,
    `action` VARCHAR(100) NOT NULL,
    `detail` TEXT NOT NULL,
    `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_timestamp` (`timestamp`),
    INDEX `idx_username` (`username`),
    INDEX `idx_action` (`action`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 📁 文件说明

| 文件 | 作用 |
|------|------|
| `includes/OperationLogger.php` | 日志记录器（数据库版本） |
| `admin/diagnosis/operation_log.php` | 日志查看页面 |
| `create_operation_logs_table.php` | 创建数据表脚本 |
| `memory/操作日志数据库设置.md` | 本文档 |

---

## 🔧 自动清理机制

每次访问日志页面时，系统会自动：
1. 检查是否有 30 天前的数据
2. 如果有，自动删除
3. 保持数据库整洁

清理 SQL：
```sql
DELETE FROM operation_logs 
WHERE timestamp < DATE_SUB(NOW(), INTERVAL 30 DAY);
```

---

## 💡 使用提示

1. **首次使用** - 先访问创建表脚本
2. **每次访问** - 都需要输入密码
3. **批量操作** - 勾选日志后可复制或删除
4. **自动清理** - 无需手动维护

---

## 📝 日志记录点

### 已实现的记录点
- ✅ 页面访问（所有菜单点击）
- ✅ 岗位管理（创建/更新/删除/切换状态）
- ✅ 客户管理（创建/更新/删除）
- ✅ 用户管理（创建/更新/删除）
- ✅ 文章管理（创建/更新/删除）

### 记录步骤
- ✅ 提交数据 - 数据验证通过
- ✅ 操作成功 - 显示 ID 和详情
- ✅ 操作失败 - 显示失败原因
- ✅ 确认操作 - 删除/切换前的确认

---

## 🎯 访问地址

- **创建表**: `http://localhost/qianwutong/create_operation_logs_table.php`
- **查看日志**: `http://localhost/qianwutong/admin/diagnosis/operation_log.php`
- **岗位管理**: `http://localhost/qianwutong/admin/position.php`

---

**创建时间**: 2026-04-08 22:10
**版本**: v2.0 (数据库版)
